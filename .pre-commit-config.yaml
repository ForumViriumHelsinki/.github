# This repo is public: gitleaks runs the default secret rules plus the
# fvh-gcp-project-id rule from .gitleaks.toml. lint.yml runs the same config
# over the whole tree.
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: 83d9cd684c87d95d656c1458ef04895a7f1cbd8e # frozen: v8.30.1
    hooks:
      - id: gitleaks
